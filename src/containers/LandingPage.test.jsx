/* eslint-disable max-len */
// import React from 'react';
// import { render, screen, waitFor } from '@testing-library/react';
// import userEvent from '@testing-library/user-event';
// import LandingPage from './LandingPage';

describe('LandingPage Container', () => {
  it('displays the Landing page', () => {
    // render(<LandingPage />);

    // const urlSearch = screen.getAllByPlaceholderText('url');
    // const crudMethod = screen.getByLabelText('get');
    // const button = screen.getByText('send');

    // userEvent.type(urlSearch, 'http://musicbrainz.org/ws/2/recording?release=281e42d6-389e-4224-9bc6-84220a62d50f&fmt=json');
    // userEvent.click(crudMethod);
    // userEvent.click(button);

    // return waitFor(() => {
    //   expect(true.toEqual(true));
    // });
  });
});
